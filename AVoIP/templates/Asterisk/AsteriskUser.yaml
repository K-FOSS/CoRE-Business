{{- if .Values.asterisk.enabled }}
{{- $fullName := include "avoip.fullname" . -}}
{{- $terraformProvider := default (printf "psql-%s-%s" .Values.datacenter .Values.region) .Values.asterisk.cdr.terraformProvider -}}
{{- $crossplaneProvider := default (printf "psql-%s-%s" .Values.datacenter .Values.region) .Values.asterisk.cdr.crossplaneProvider -}}
---
apiVersion: mylogin.space/v1alpha1
kind: User
metadata:
  name: {{ $fullName }}-asterisk

spec:
  name: 'Asterisk'
  serviceAccount: true
  groups:
    - 'LDAPService'
{{- if .Values.asterisk.cdr.enabled }}
  psql:
    enabled: true
    crossplane:
      terraformProvider: '{{ $terraformProvider }}'
      crossplaneProvider: '{{ $crossplaneProvider }}'
{{- end }}

  writeConnectionSecretToRef:
    name: '{{ $fullName }}-asterisk'
{{- end }}
